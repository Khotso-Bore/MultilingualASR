# Python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# Environment / secrets
.env

# OS
.DS_Store

# Regeneratable audio (raw downloads, VAD/resampled output)
dataset/raw/
dataset/processed/

# Experiment outputs: model checkpoints stay local (large, regeneratable
# from a training run), but run logs are tracked - they're small and are the
# evidence trail for the report (see results/logs/README.md)
results/*
!results/logs/
!results/logs/**
