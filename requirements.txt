# Core - data acquisition, EDA
datasets>=2.19.0
pandas>=2.2.0
numpy>=1.26.0
soundfile>=0.12.1
matplotlib>=3.8.0

# Required by datasets>=4.x to decode audio columns
torch
torchcodec

# Audio preprocessing (VAD segmentation of long ANV clips)
# webrtcvad still imports pkg_resources, which setuptools>=81 removed
webrtcvad>=2.0.10
setuptools<81

# Jupyter
jupyter>=1.0.0
ipykernel>=6.29.0

# Add ASR (transformers, jiwer, peft, accelerate) and classification
# (transformers, evaluate) dependencies here as those pipeline stages land.
